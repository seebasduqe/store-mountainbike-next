/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['example.com', 'another-domain.com', 'images.example.org','www.pexels.com', 'images.pexels.com'],
      },
};

export default nextConfig;
